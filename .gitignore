node_modules/
.env
.env.local
.next/
dist/
.turbo/
*.log
agentroom_graph_*.html
*.tsbuildinfo
*.lnk
apps/web/.claude/
bridge/.tmp-test/

# Internal AI/build-process tooling — untracked from the public repo but kept on disk (the local
# dev workflow still works) and preserved in git history + the local `internal/build-tooling`
# branch. A public cloner doesn't need any of it.
CLAUDE.md
.claude/
docs/production-hardening/
docs/reviews/
scripts/agent-runner.ps1
scripts/register-task.ps1

# hardening scratch: stale worktrees, graphify output
.worktrees/
graphify-out/

# Coverage output
coverage/
.nyc_output/

# Playwright e2e artifacts
playwright-report/
test-results/

# Lighthouse / authed user-flow + UX-audit scratch (local dev only)
.lh-profile/
lh-*.json
._*.mjs
