# QA / Verification Review — ADR-0011 `/discuss` team collaboration (`/critique qa`)

Date: 2026-06-01 · Scope: `git diff feat/product-validation-v1...HEAD`.

## Test execution (independently re-run)
- shared 10/10 · orchestrator 9/9 (→11/11 after guardrail tests) · hallucination 5/5 · typecheck 0.
- Tests are **real** (would catch regressions): the integrate→dissent/converge tests assert the exact
  inserted `phase` driven by `challengePresent`; idempotency asserts `messages.length===0`; the
  hallucination test constructs the exact 2-contradiction input that previously yielded
  `confidence==='medium'`.

## Termination proof (no non-terminating path)
1. **DAG** — `nextDiscussionStage` is a back-edge-free switch; all discuss paths end at `converge`,
   debate at `adjudicate`, both return `null`. Longest path plan→execute→integrate→dissent→converge.
2. **Phase budget** — `DISCUSSION_MAX_PHASES=5` (converge = stage 5).
3. **Absolute round ceiling** — `ABS_MAX_DISCUSSION_ROUNDS=6` (longest path reaches round 4).
4. **Idempotency** — partial unique index makes each phase insertable once; `23505` race-loser path
   returns cleanly. Anti-sycophancy cannot loop: `dissent → converge` is unconditional, dissent is
   inserted at most once (unique index), converge is terminal.

## Triage + disposition

| SEV | Title | Gap / risk | Disposition |
|---|---|---|---|
| Med | `COLLAB_MAX_AGENTS` defined but not enforced | a >3-agent room over-fans (4+4+4+1 instead of ≤11) | **FIXED** (7ef7e9e): orchestrator caps the tight loop to 3 (keeps coordinator) + test |
| Med | `anti_sycophancy` flag in type/ADR but never written | silent rubber-stamp leaves no audit trail | **FIXED** (7ef7e9e): stamped on converge-after-dissent-with-no-challenge + test |
| Med | 1-agent room → incoherent integrate/dissent (no peer to review) | flow doesn't hang but is meaningless solo | **DEFER** — out of the team tight-loop scope; tracked for the scaling sprint |
| Med | partial phase failure (1 ok + N failed) advances; intent untested | future change could silently regress best-effort intent | **DEFER** — add an intent-locking test next sprint |
| Low | orphaned phase message if `agent_runs` insert fails after message insert (non-atomic) | discussion could stick (idempotency pre-check then blocks retry) | **DEFER** — low-probability transient; extend stale-run recovery later |
| Low | `fakeCite` regex false-positive for whitespace-separated URL | pre-existing (predates this campaign); banner over-flags | **DEFER** — pre-existing; track separately |
| Low | coordinator-leaves-mid-discussion fallback untested | rare; code is correct (re-derives) | **DEFER** — robustness test nice-to-have |

No gap is a hang, infinite loop, data loss, or security regression. **Safe to merge.**
